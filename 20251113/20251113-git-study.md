# 📅 2025-11-13 TIL — Git Workflow & Branch Practice

## 1. Today’s Summary
- 로컬 → 스테이징 → 로컬 리포지토리 → GitHub까지 전체 흐름 체득
- add/commit/push 과정 완전 익힘
- first-repo Commit 메시지 기반 기능 구현 실습
- branch-practice 레포 클론 후 fizzbuzz 실습
- pre-commit 훅 실행 및 검사 통과 과정 확인
- Python IndentationError 디버깅 수행

## 2. What I Did
### (1) Git 기본 작업 흐름 실습
- Working Directory → Staging Area → Local Repo → Remote Repo 구조 이해  
- git add / git commit / git push 실습  
- git status로 작업 상태 점검

### (2) first-repo 실습
- feat: Create adder.py — 빈 파일 생성
- feat: Add sum feature — add(a, b) 함수 구현
- fix: Rename Function — add → adder로 변경
- GitHub에 반영 확인

### (3) branch-practice 실습
- git clone으로 리포지토리 복제
- fizzbuzz.py 작성
- IndentationError 해결 (탭/스페이스 문제 정리)
- 정상 실행 확인

### (4) pre-commit 실습
- whitespace trim, yaml check, file size check 등 hook 실행
- Staged 상태와 commit의 차이 이해

## 3. Key Learnings
### Git Workflow
Working Directory
↓ git add
Staging Area
↓ git commit
Local Repository
↓ git push
Remote Repository (GitHub)

### Python IndentationError 원인
- 혼합된 탭/스페이스
- 빈 줄의 공백
- 복사한 코드의 hidden character

### Git 오류 처리 경험
- staged 상태만 있고 commit이 없으면 push 돼도 변화 없음
- 경로 오타(pathspec) 분석
- branch 이동 시 파일 변화 확인

## 4. Commands Used
git clone https://github.com/Jo-Baek/branch-practice.git

git add .
git commit -m "docs: Add TIL for 2025-11-13"
git push origin main
git status
git log --oneline
python fizzbuzz.py

## 5. Issues & Debugging
| Issue | Cause | Solution |
|------|--------|-----------|
| push했는데 GitHub에 파일 안 뜸 | commit 없음 | commit 후 push |
| pathspec 오류 | 폴더명 오타(`pytho`) | 정확하게 입력 |
| IndentationError | hidden whitespace | 들여쓰기 재정렬 |
| pre-commit 충돌 | formatting 자동 수정됨 | 다시 add 후 commit |

## 6. Next Step
- Merge conflict 실습
- Git Flow vs Trunk-based 비교 학습
- Python 자동 포맷팅 도구(black, flake8) 적용
- pre-commit custom rule 만들기

## 7. Reference
- first-repo / branch-practice GitHub 레포
- Git 공식 문서

